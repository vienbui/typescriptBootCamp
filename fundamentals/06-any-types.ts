
let lessonCount:any = 10;

let number:any[] = [1,2,3, "Hello", true];

printCourse(10, "Typescript Basics");

function printCourse(title:string, lessonCount){
    console.log(`Course ${title} has ${lessonCount} lessons.`);
}